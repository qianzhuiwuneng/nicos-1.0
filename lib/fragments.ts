/** Short notes / quotes / half-formed thoughts — extend as you like. */
export interface FragmentEntry {
  id: string;
  body: string;
  createdAt: string;
}

export const fragmentEntries: FragmentEntry[] = [];
